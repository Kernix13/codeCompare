// each const variable name = section#id
// Each key = the value for input type="checkbox"
// The selected languages values are saved to LS, but the Primary is saving the select option text value
// This is a bad file name
const vars = {
  "javascript": [
    'let varName = value;',
    'const PHI = 1.618;',
    'null',
    'true, false',
    '==, !=, ===, !==',
    'a && b',
    'a || b',
    '!x',
    'console.log();',
    'typeof',
    'instanceof',
    '// single line',
    '/* multi-line */',
  ],
  "php": [
    '$varName = value;',
    'define("PHI", 1.618);',
    'null',
    'true, false',
    '==, !=, ===, !==',
    '$a && $b',
    '$a || $b',
    '!$x',
    'echo',
    'gettype()',
    'instanceof',
    '// single line',
    '/* multi-line */',
  ],
  "python": [
    'var_name = value',
    'PHI = 1.618',
    'None',
    'True, False',
    '==, !=',
    'a and b',
    'a or b',
    'not x',
    'print()',
    'type()',
    'isinstance()',
    '# single line',
    '""" multi-line """',
  ],
  "csharp": [
    'var varName = value;',
    'const double PHI = 1.618;',
    'null',
    'true, false',
    '==, !=',
    'a && b',
    'a || b',
    '!x',
    'Console.WriteLine(obj);',
    'obj.GetType()',
    'obj is SomeClass',
    '// single line',
    '/* multi-line */',
  ],
  "dart": [
    'var varName = value;',
    'final PHI = 1.618;',
    'null',
    'true, false',
    '==, !=',
    'a && b',
    'a || b',
    '!x',
    'print()',
    'varName.runtimeType',
    'obj is SomeClass',
    '// single line',
    '/* multi-line */',
  ],
}

const numbers = {
  "javascript": [
    'Infinity',
    'NaN',
    'Math.min(...nums)',
    'Math.max(...nums)',
    'Math.floor(x)',
    'Math.random()',
    'Math.sqrt(x)',
    'Math.pow(base, exp)',
    'parseInt("num", base = 10)',
    'parseFloat(string)',
    'toFixed(digits)',
    'toPrecision(digits)',
    'isInteger(val)',
    'isNaN(val)',
    '+= -= *= /=',
    '% ** ++ --',
    'num++',
  ],
  "php": [
    'INF',
    '-',
    'min($a, $b)',
    'max($a, $b)',
    'floor($n)',
    'rand(min, max)',
    'sqrt($n)',
    'pow($a, $b)',
    'intval($num, base=10)',
    'floatval($str)',
    "number_format($num, $digits, '.', '')",
    'sprintf("%.{$digits}g", $num)',
    'is_int($val)',
    'is_nan($val)',
    '+= -= *= /=',
    '% ** ++ --',
    '$num++',
  ],
  "python": [
    'float("inf")',
    'float("nan")',
    'min(a, b)',
    'max(a, b)',
    'math.floor(n)',
    'random.random()',
    'import math | math.sqrt(n)',
    'pow(a, b) | a ** b',
    'int(num, base = 10)',
    'float(str)',
    'f"{num:.2f}"',
    'f"{num:.{digits}g}"',
    'isinstance(val, int)',
    'math.isnan(val)',
    '+= -= *= /=',
    '% ** //',
    'num += 1',
  ],
  "csharp": [
    'double.PositiveInfinity',
    'double.NaN',
    'Math.Min(a, b)',
    'Math.Max(a, b)',
    'Math.Floor(x)',
    'Random.NextDouble()',
    'Math.Sqrt(x)',
    'Math.Pow(base, exp)',
    'int.Parse("num")',
    'double.Parse(str)',
    'num.ToString($"F{digits}")',
    'num.ToString($"G{digits}")',
    'val is int',
    'double.IsNaN(val)',
    '+= -= *= /=',
    '% ** ++ --',
    'num++;',
  ],
  "dart": [
    'double.infinity',
    'double.nan',
    'n.reduce((a, b) => a < b ? a : b)',
    'n.reduce((a, b) => a > b ? a : b)',
    'x.floor()',
    'Random().nextDouble()',
    'sqrt(x)',
    'pow(base, exp)',
    'int.parse(str, rad: rad)',
    'double.parse(string)',
    '',
    '',
    '',
    '',
    '+= -= *= /=',
    '% ** ++ --',
    'num++',
  ],
}

const strings = {
  "javascript": [
    '`Hello ${name}`',
    'str.length',
    'str.indexOf(sub)',
    'str[i]',
    'str + str2',
    'str.match(/sub/g)?.length',
    '+=',
    'str.slice(start, end)',
    'str.substring(start, end)',
    'str.split(sep)',
    'str.replace("old", "new")',
    'str.replace(regex, "new")',
    'str.match(regex)',
    'str.toLowerCase()',
    'str.toUpperCase()',
    'str.trim()',
    'String(num)',
    '--BOOLEAN--',
    'str.startsWith(sub)',
    'str.endsWith(sub)',
    'str.includes(sub)',
  ],
  "php": [
    '"Hello {$name}"',
    'strlen($str)',
    'strpos($str, $char)',
    '$str[$i]',
    '$str . $str2',
    'substr_count($str, $substr)',
    '.=',
    'substr($str, start, +/-len)',
    'substr($str, start, len)',
    'explode(sep, $str)',
    'str_replace("old", "new", $str);',
    'preg_replace("regex", "new", $str);',
    'preg_match(pattern, $str, $matches)',
    'strtolower($str)',
    'strtoupper($str)',
    'trim($text)',
    'strval($num)',
    '--BOOLEAN--',
    'str_starts_with($str, $char)',
    'str_ends_with($str, $char)',
    'str_contains($str, $substr)',
  ],
  "python": [
    'f"Hello {name}"',
    'len(txt)',
    'txt.find(sub)',
    'txt[index]',
    'txt + txt',
    'txt.count(substr)',
    '+=',
    'txt[start:end]',
    'txt[start:end]',
    'txt.split(sep)',
    'txt.replace("old", "new")',
    're.sub(r"pattern", "new", txt)',
    're.search(pattern, txt)',
    'txt.lower()',
    'txt.upper()',
    'txt.strip()',
    'str(num)',
    '--BOOLEAN--',
    'txt.startswith(sub)',
    'txt.endswith(sub)',
    'sub in txt',
  ],
  "csharp": [
    '$"Hello {name}"',
    'str.Length',
    'str.IndexOf(sub)',
    'str[i]',
    'str + str2',
    'str.Count(c => c == "x")',
    '+=',
    'str.Substring(beg, end - beg)',
    'str.Substring(start, len)',
    'str.Split(sep)',
    'str.Replace("old", "new");',
    'Regex.Replace(str, "regex", "new");',
    'Regex.Match(str, pattern)',
    'str.ToLower()',
    'str.ToUpper()',
    'str.Trim()',
    'num.ToString()',
    '--BOOLEAN--',
    'str.StartsWith(sub)',
    'str.EndsWith(sub)',
    'str.Contains(sub)',
  ],
  "dart": [
    '"Hello $name"',
    'str.length',
    'str[i]',
    'str + str2',
    '+=',
    'str.substring(start, end)',
    'str.substring(start, end)',
    'str.split(sep)',
    'str * count',
    'str.replaceAll(RegExp(reg), sub)',
    'str.toLowerCase()',
    'str.toUpperCase()',
    'str.trim()',
    'num.toString()',
    '--BOOLEAN--',
    'str.startsWith(sub)',
    'str.endsWith(sub)',
    'str.contains(sub)',
    'str.indexOf(sub)',
  ],
}

const arrays = {
  "javascript": [
    'Array.from(iterable[, cb])',
    'arr.length',
    'arr[i]',
    'arr[i][j]',
    'arr2 = [...arr1]',
    'arr.join(sep)', 
    'arr1.concat(arr2)',
    'arr.filter(cb)',
    'arr.reduce(cb)',
    'arr.includes(item)',
    'arr.indexOf(item)',
    'arr.forEach((item, i))',
    'arr.map((item, i))', 
    'arr.slice(start, end)',
    '--MUTATING--',
    'arr.length = 0',
    'arr.push(item)', // Mutating
    'arr.unshift(item)',
    'arr.pop()',
    'arr.shift()',
    'arr.reverse()',
    'arr.sort()',
    'arr.splice(i, 0, item)',
    'arr.splice(i, 1)',
    'arr.splice(i, 1, item)',
    'arr.push(...arr2)',
    'arr.splice(i, 1)',
  ],
  "php": [
    '-',
    'count($arr)',
    '$arr[i]',
    '$arr[i][j]',
    '$arr2 = [...$arr1];',
    'implode($sep, $arr)',
    'array_merge($arr1, $arr2)',
    'array_filter($arr, "cb")',
    'array_reduce($arr, "cb")',
    'in_array($item, $arr)',
    'array_search($item, $arr)',
    'foreach ($arr as $val) {...}',
    'array_map("cb", $arr)',
    'array_slice($arr, start, len)',
    '--MUTATING--',
    '$arr = [];',
    'array_push($arr, "vals")',
    'array_unshift($arr, "vals")',
    'array_pop($arr)',
    'array_shift($arr)',
    'array_reverse($arr)',
    'sort($arr)',
    'array_splice($arr, i, 0, item)',
    'array_splice($arr, i, 1)',
    'array_splice($arr, i, 1, item)',
    'array_merge($arr1, $arr2)',
    '-',
  ],
  "python": [
    'list(iterable)',
    'len(list_name)',
    'list[i]',
    'list[i][j]',
    'l2 = l1.copy(), l2 = l1[:]',
    'sep.join(list)',
    'list1 + list2',
    '[x for x in list if x expr]',
    '-',
    'if item in list:',
    'list.index(item)',
    '-',
    '[x expr for x in list]',
    'list[start:end]',
    '--MUTATING--',
    'list.clear()',
    'list.append(val)',
    'list.insert(0, item)',
    'list.pop(i)',
    'list.pop(0)',
    'list.reverse()',
    'sorted(iterable)',
    'list.insert(i, item)',
    'list.pop(i)',
    'list[i] = item',
    'list1.extend(list2)',
    'list.remove(x)',
  ],
  "csharp": [
    'new List<T>(iterable)',
    'list1.Length or list1.Count',
    'list1[i]',
    'list1[i,j] | list1[i][j]',
    'var list2 = new List<T>(list1)',
    'string.Join(sep, list1)',
    'list1.Concat(list2).ToList()',
    'list1.Where(cb).ToList()',
    'list1.Aggregate(cb)',
    'list1.Contains(item)',
    'list1.IndexOf(item)',
    'list1.ForEach((item) => {...})',
    'list1.Select(cb).ToList()',
    'list1.GetRange(beg, end-beg)',
    '--MUTATING--',
    'list1.Clear()',
    'list1.Add(item)',
    'list1.Insert(0, item)',
    'list1.RemoveAt(list1.Count - 1)',
    'list1.RemoveAt(0)',
    'list1.Reverse()',
    'list1.Sort()',
    'list1.Insert(i,item)',
    'list1.RemoveAt(i)',
    'list1[i] = item',
    'list1.AddRange(list2)',
    'list1.Remove(x)',
  ],
  "dart": [
    'list1.insert(0, item)',
    'list1.length',
    'list1[i]',
    'list2 = [...list1]',
    'list1.join(sep)',
    'list1 + list2',
    'list1.where(cb).toList()',
    'list1.reduce(cb)',
    'list1.contains(item)',
    'list1.indexOf(item)',
    '--too long--',
    '--too long--',
    'list1.add(item)',
    '--MUTATING--',
    'list1.clear()',
    'list1.removeLast()',
    'list1.removeAt(0)',
    '--too long--',
    'list1.sort()',
    'list1.sublist(start, end)',
    'List.from(iterable)',
    'list1.insert(i, item)',
    'list1.removeAt(i)',
    'list1[i] = item',
    'list1.addAll(list2)',
    'list1.remove(x)',
  ],
}

const objects = {
  "javascript": [
    'const obj = { key: val };',
    'obj.key; | obj["key"];',
    'obj["key"] = val',
    'delete obj["key"];',
    'obj.hasOwnProperty(key);',
    'Object.entries(obj);',
    'Object.keys(obj);',
    'Object.values(obj);',
    'obj3 = {...obj1, ...obj2}',
    'const {name, email} = userObj;'
  ],
  "php": [
    '$arr = ["key" => $val];',
    '$arr["key"];',
    '$arr["key"] = val;',
    'unset($arr["key"]);',
    'array_key_exists($key, $arr);',
    '-',
    'array_keys($arr);',
    'array_values($arr);',
    '$arr3 = $arr1 + $arr2;',
    '["name" => $name, "email" => $email] = $user;'
  ],
  "python": [
    'dict1 = {"key": val}',
    'dict1["key"]',
    'dict1["key"] = val',
    'del dict1["key"]',
    'if key in dict1:',
    'dict1.items()',
    'dict1.keys()',
    'dict1.values()',
    'dict3 = {**dict1, **dict2}',
    'name = user["name"] | email = user["email"]'
  ],
  "csharp": [
    'new Dictionary<string, object> { ["key"] = val };',
    'dict1["key"]',
    'dict1["key"] = val;',
    'dict1.Remove("key");',
    'dict1.ContainsKey("key");',
    'dict1.ToList()',
    'dict1.Keys',
    'dict1.Values',
    '--too long--',
    'N/A'
  ],
  "dart": [
    '--too long--',
    'map1["key"]',
    'map1["key"] = val',
    'map1.remove("key");',
    'map1.containsKey("key");',
    'map1.entries.toList()',
    'map1.keys.toList()',
    'map1.values.toList()',
    'var map3 = {...map1, ...map2};',
    ''
  ],
}

// This one needs work cbut is meant to show important methods/props
// that are unique to a language with no real correspondence in other languages
const uniques = {
  "javascript": [
    'console.dir',
    'console.trace',
    'Number(str)',
    'padEnd',
    'padStart',
    'replaceAll',
    'match(regex)',
    'every',
    'some',
    'find',
    'findIndex',
    'e.target.value',
    'DOM methods',
    'addEventListener',
    'stringify',
    'JSON.parse',
    '',
  ],
  "php": [
    'var_dump',
    'intval',
    'substr_count',
    'number_format',
    'count_chars',
    'unset',
    'array_combine',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
  ],
  "python": [
    'input()',
    '10 // 3',
    'range(start, end)',
    'fromkeys',
    'dict1.get()',
    'dict1.update()',
    'dict1.clear()',
    'dict2 = dict1.copy()',
    'chr()',
    'all',
    'any',
    'isdecimal',
    'isnumeric',
    'count',
    '[expr for item in list]',
    '[expr for item in list if cond]',
    'tuple1 + tuple2',
  ],
  "csharp": [
    'Remove',
    'ReadLine',
    'Substring',
    'TrimStart',
    '$""" {a} """',
    'Insert',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
  ],
  "dart": [
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
    '',
  ],
}